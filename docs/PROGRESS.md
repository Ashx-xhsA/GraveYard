# 进度

> **只回答「现在在哪」。保持极短（十几行），每次 `/exe` 收尾时刷新。**
> 整体怎么走看 `ROADMAP.md`，具体做哪些事看 `TODO.md`（编号 #n 即 TODO 里的编号）。

---

**当前阶段**：Phase 1 · 地基冻结 —— **代码部分已全部完成，迁移已在线上 `test` 库执行**，**浏览器验收已通过**，待合并部署

**上次做完**：
- 2026-09-19 · Phase 0 代码全部完成（#1 #2 #3 #5 #18 #19 #41 #42，#4 跳过），PRD D12–D16
- 2026-09-25 · **Phase 1 #6–#14 全部落地**：新增 `client/src/types.ts`（组件里的 `any` 已清零）、`server/src/lib/graveDetail.js`（统计算法统一、user/block 统一 populate）、`server/migrate-phase1.js`（已 `--apply`，备份在 `server/backups/`）。路由参数统一为 `:graveID` / `:blockID`（PRD **D17**）。新立 **#44** lint 清零

**👉 下一步起点**：
1. ✅ 浏览器验收已通过（2026-09-25，含 Phase 0 四条和「未登录隐藏入口」）
2. 推送并合并到 `main` → 线上复验详情页。⚠️ **合并之前，线上详情页的记录会显示为空**（数据已经是新格式，代码还是旧的）
3. 之后进 **Phase 2 · 核心玩法**（#27 #28 #29 #37）

**卡住 / 待决定**：无

**当前分支**：`refactor/phase1-foundation`（已提交 4 个 commit，未推送）
